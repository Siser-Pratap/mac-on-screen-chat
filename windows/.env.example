# Copy this file to `.env` and fill in your key. `.env` is gitignored.
# Used only locally; build-app.ps1 copies it into %LOCALAPPDATA%\OnScreenChat\.
#
# The Windows build has no local models, so this key is required for replies.

GEMINI_API_KEY=your-gemini-api-key-here
